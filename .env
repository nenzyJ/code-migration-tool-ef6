# The variables added in this file are only available locally in MiniOxygen.
# Run `pnpm shopify hydrogen link` to also inject environment variables from your storefront,
# or `pnpm shopify hydrogen env pull` to populate this file.
SESSION_SECRET="foobar"
